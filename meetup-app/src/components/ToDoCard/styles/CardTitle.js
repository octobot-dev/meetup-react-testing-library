import styled from 'styled-components';

import { PRIMARY_COLORS } from '../../../resources/colors';

const CardTitle = styled.label`
    color: ${PRIMARY_COLORS.subtitle};
    font-weight: bold;
    font-style: normal;
    font-size: 18px;
    line-height: 20px;
    margin: 0px;
`;


export default CardTitle;
